DATABASE_URL=sqlite:///./storage/wrapready.db
REDIS_URL=redis://localhost:6379/0
STORAGE_PATH=./storage
MAX_UPLOAD_SIZE_MB=50
TARGET_DPI=120
MIN_DPI=100
MAX_DPI=150
CORS_ORIGINS=http://localhost:3000

# Frontend (Next.js) — set this to your deployed backend URL in production.
# In production (e.g. Vercel), add NEXT_PUBLIC_API_URL to your environment variables.
# Example: NEXT_PUBLIC_API_URL=https://api.wrapreadyai.com
NEXT_PUBLIC_API_URL=http://localhost:8000
