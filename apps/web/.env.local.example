# Leave unset for local dev — Next.js proxies /api/* to localhost:8000 automatically.
# Set this only when pointing at a remote API (e.g. production or staging).
# NEXT_PUBLIC_API_URL=https://api.example.com
